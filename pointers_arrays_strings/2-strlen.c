#include "main.h"
/**
* _strlen - functio for see size variable.
* @s: pointer.
* Description: see variable size.
* Return: 0
*/
int _strlen(char *s)
{
	int T;

	for (T = 0; s[T] != '\0'; T++)
	{
	}
	return (T);
}

