#include <stdio.h>
/**
 * function (main) for start the program
 * Description: using if else while
 * Return = 0
 */
int main (void)
{
	char L = 'a';
	for(L = 'a'; L <= 'z'; L++)
	{
		putchar(L);
	}

	putchar('\n');
	return (0);
}

